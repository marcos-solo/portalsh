# Aiven / External DB URI (Option 1)
# DATABASE_URL=mysql://avnadmin:password@host:port/defaultdb?ssl-mode=REQUIRED

# Aiven / External DB individual variables (Option 2)
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=ccnaportal
DB_USER=root
DB_PASS=
DB_DIALECT=mysql
DB_SSL=false

JWT_SECRET=change_this_secret
PORT=5000
UPLOAD_DIR=uploads

